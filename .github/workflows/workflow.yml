# Display name of the workflow
name: Validate and tests

# Trigger conditions
on:
  push

jobs:
  # First job
  my-job:
    # Runner
    runs-on: ubuntu-latest
    # List of steps
    steps:
      - name: My Step
        run: echo "Hello World"
